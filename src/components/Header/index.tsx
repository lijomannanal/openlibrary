import React from 'react';
import './style.scss';

const Header = () => (
  <header className="app-header">
    <div className="app-title">
      <h1>Open Library</h1>
    </div>
  </header>
);

export default Header;
